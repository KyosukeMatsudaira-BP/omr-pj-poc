[tool.poetry]
name = "src"
version = "0.1.0"
description = "Source package for OMR project"
authors = ["Your Name <your.email@example.com>"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"